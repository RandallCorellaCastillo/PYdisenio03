import React from 'react';
import './BottomBar.css';

const BottomBar = () => {
  return (
    <div className="bottom-bar">
      {/* Contenido de la barra inferior */}
      Este es el contenido de la barra inferior.
    </div>
  );
};

export default BottomBar;